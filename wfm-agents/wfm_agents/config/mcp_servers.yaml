# MCP server entries for WFM ToolGateway (ARCH §3.5). Hot reload: POST /v1/admin/mcp/reload
# Override path with env WFM_MCP_CONFIG. Empty list = builtin tools only.
#
# Example (stdio):
#   servers:
#     - id: example
#       transport: stdio
#       command: uvx
#       args: ["mcp-server-filesystem", "/path"]
#       env: {}
#       risk_tier: read
#
# Example (sse):
#   servers:
#     - id: remote
#       transport: sse
#       url: "http://127.0.0.1:3001/sse"
#       risk_tier: read

servers: []
